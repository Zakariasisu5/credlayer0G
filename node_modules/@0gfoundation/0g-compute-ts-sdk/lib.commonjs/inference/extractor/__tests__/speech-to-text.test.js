"use strict";
// Unit tests for the speech-to-text usage extractor's dual billing modes.
// The dispatch rules must stay in sync with the broker's settlement logic
// (isDurationUsage / billableSeconds in 0g-serving-broker): the cached fee
// estimate the SDK computes drives auto top-up, and a divergence between the
// two re-introduces the zero-fee bug class the broker fixed in PR #523.
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const mocha_1 = require("mocha");
const speech_to_text_1 = require("../speech-to-text");
const svcInfo = {};
const extractor = new speech_to_text_1.SpeechToText(svcInfo);
(0, mocha_1.describe)('SpeechToText extractor', () => {
    (0, mocha_1.describe)('duration-billed usage (whisper family)', () => {
        (0, mocha_1.it)('uses seconds as input count', async () => {
            const usage = JSON.stringify({ type: 'duration', seconds: 207 });
            (0, chai_1.expect)(await extractor.getInputCount(usage)).to.equal(207);
            (0, chai_1.expect)(await extractor.getOutputCount(usage)).to.equal(0);
        });
        (0, mocha_1.it)('rounds fractional seconds to the nearest second', async () => {
            (0, chai_1.expect)(await extractor.getInputCount(JSON.stringify({ type: 'duration', seconds: 207.5 }))).to.equal(208);
            (0, chai_1.expect)(await extractor.getInputCount(JSON.stringify({ type: 'duration', seconds: 207.4 }))).to.equal(207);
        });
        (0, mocha_1.it)('floors sub-second positive durations to 1 second', async () => {
            (0, chai_1.expect)(await extractor.getInputCount(JSON.stringify({ type: 'duration', seconds: 0.4 }))).to.equal(1);
        });
        (0, mocha_1.it)('returns 0 for zero or negative seconds', async () => {
            (0, chai_1.expect)(await extractor.getInputCount(JSON.stringify({ type: 'duration', seconds: 0 }))).to.equal(0);
            (0, chai_1.expect)(await extractor.getInputCount(JSON.stringify({ type: 'duration', seconds: -5 }))).to.equal(0);
        });
        (0, mocha_1.it)('routes to duration when seconds is populated without a type field', async () => {
            const usage = JSON.stringify({ seconds: 42 });
            (0, chai_1.expect)(await extractor.getInputCount(usage)).to.equal(42);
            (0, chai_1.expect)(await extractor.getOutputCount(usage)).to.equal(0);
        });
    });
    (0, mocha_1.describe)('token-billed usage (gpt-4o-transcribe family)', () => {
        (0, mocha_1.it)('uses input_tokens and output_tokens', async () => {
            const usage = JSON.stringify({
                type: 'tokens',
                input_tokens: 120,
                output_tokens: 30,
            });
            (0, chai_1.expect)(await extractor.getInputCount(usage)).to.equal(120);
            (0, chai_1.expect)(await extractor.getOutputCount(usage)).to.equal(30);
        });
        (0, mocha_1.it)('stays on the tokens path even with a stray seconds field', async () => {
            const usage = JSON.stringify({
                type: 'tokens',
                input_tokens: 120,
                output_tokens: 0,
                seconds: 99,
            });
            (0, chai_1.expect)(await extractor.getInputCount(usage)).to.equal(120);
            (0, chai_1.expect)(await extractor.getOutputCount(usage)).to.equal(0);
        });
        (0, mocha_1.it)('accepts string-encoded token counts', async () => {
            const usage = JSON.stringify({
                type: 'tokens',
                input_tokens: '120',
                output_tokens: '30',
            });
            (0, chai_1.expect)(await extractor.getInputCount(usage)).to.equal(120);
            (0, chai_1.expect)(await extractor.getOutputCount(usage)).to.equal(30);
        });
    });
    (0, mocha_1.describe)('malformed usage', () => {
        (0, mocha_1.it)('returns 0 for empty content', async () => {
            (0, chai_1.expect)(await extractor.getInputCount('')).to.equal(0);
            (0, chai_1.expect)(await extractor.getOutputCount('')).to.equal(0);
        });
        (0, mocha_1.it)('returns 0 for invalid JSON', async () => {
            (0, chai_1.expect)(await extractor.getInputCount('not json')).to.equal(0);
            (0, chai_1.expect)(await extractor.getOutputCount('not json')).to.equal(0);
        });
        (0, mocha_1.it)('returns 0 for an empty usage object', async () => {
            (0, chai_1.expect)(await extractor.getInputCount('{}')).to.equal(0);
            (0, chai_1.expect)(await extractor.getOutputCount('{}')).to.equal(0);
        });
    });
});
//# sourceMappingURL=speech-to-text.test.js.map